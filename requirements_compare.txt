# Environment for compare_starmap.py (spatialz vs UOT and other 2-way comparisons).
# Includes the SpatialZ evaluation stack plus metrics/UOT dependencies.
#
# Typical workflow:
#   pip install -r requirements_compare.txt
#   python evaluation_starmap.py spatialz   # if output/spatialz/... not present
#   python evaluation_starmap.py uot          # optional; compare can generate UOT on demand
#   python compare_starmap.py

# --- SpatialZ stack (requirements_spatialz.txt) ---
numpy==1.22.2
pandas==2.0.0
anndata==0.9.1
scikit-learn==1.2.2
scipy==1.10.1
pot==0.9.3
SOMENDER==1.1
open3d==0.18.0
scanpy==1.9.3
squidpy==1.2.3

# --- Compare metrics (SSIM plots, comparative figures) ---
matplotlib==3.6.3
scikit-image==0.19.3

# --- UOT (uot/geometry.py imports torch; h5ad I/O) ---
torch==2.0.1
h5py==3.8.0
